# 자주 사용할 명령어
- `pwd`: 현재 경로 확인(루트는 /Users/sspark/Work/rules).
- `ls` / `ls -a`: 루트 파일·폴더 빠르게 확인.
- `tree -L 2 --gitignore | cat`: gitignore 반영하여 2단계까지 구조 파악.
- `rg "패턴"`: 필요한 문자열/식별자 검색(대규모 파일 대신 국소 검색 권장).
- `cat README.md`: 저장소 목적 및 Serena 활성화 메모 확인.
- 현재 테스트/포맷/빌드 스크립트 없음. 코드 추가 시 별도 명령 정의 필요.