# report-style 규칙 개정 이력 — report-style 참조

> report-style 스킬의 규칙이 언제·왜 바뀌었는지를 담는 파일. **작성·교정 작업에는 읽지 않는다** — 규칙 자체를 고치거나, 어떤 규칙의 근거·적용 사례를 되짚을 때만 연다. `core.md` 에서 분리한 이유는 그 파일이 매 작업마다 전량 로드되는데 이 이력은 그때 쓰이지 않기 때문(2026-09-05).
>
> 항목 형식 = `- **YYYY-MM-DD HH:MM** — 무엇을 바꿨는지 · 근거 · 적용 지점 · 연동 수정`. 새 항목은 맨 위에 추가한다.

---

## 개정 이력

- **2026-09-05 22:55** — **[원문 요약(digest)] 도식·불릿 동기 + 세부 깊이 규칙 추가**. 근거 = 팀장님 지적('26.9.5, 22:50 개정으로 고친 Pipecat 요약본 §3 재리뷰) "테이블을 뺀 건 좋은데 디테일 조절은 안 한 것 같다. 그림에 표시가 안 된 컴포넌트가 나열돼 싱크도 안 맞고, 요약 관점에서 정보가 과하다". 22:50 개정은 표를 불릿으로 바꾸는 **형식**만 정했고 무엇을 남길지의 **깊이**는 정하지 않아, 표의 행 9개가 불릿 9개로 그대로 옮겨졌음(절 불릿 13개 — 규칙 2의 3~7개 상한 위반이었으나 눈에 걸리지 않음). 조치 = `도식·표` 항에 불릿 둘 ⑴ **도식과 불릿의 동기** — 구성 요소 불릿은 도식에 그려진 요소만, 핵심이면 도식에 넣고 아니면 불릿에서도 뺌, 3~7 상한은 도식이 있어도 같음 ⑵ **세부의 깊이** — 요소당 이름·역할 한 줄까지, 클래스·디렉토리 개수·도입 버전·구 이름·폐기 방향 같은 계보 세부는 0절이 언급하지 않으면 뺌(되말할 대상이 아니라 찾아볼 대상). `SKILL.md` 3단계 2번 연동. 적용 = Pipecat 요약본 §3 을 불릿 13 → 6(도식 요소 기준)으로, 소스 좌표는 `참고` 절 한 줄 유지
- **2026-09-05 22:50** — **[원문 요약(digest)] 표 허용 조건에 "셀 해상도" 기준 신설 + 팩트시트 유형 추가**. 근거 = 팀장님 판정('26.9.5, `scratch/VoiceAgent/DIGEST-PIPECAT-2026-09-05-1852.md` 리뷰) — §2 유지보수 주체 표(항목·값)는 "정보를 요약해 일목요연하게 정리해서 좋다", §3 아키텍처 표(구성 요소 × `src/` 소재 × 역할)는 "실제 소스의 패키지·모듈명까지 나열해 요약 관점에서 적절치 않은 디테일". 기존 규칙의 구멍 두 개 — ⑴ 좋다고 판정된 §2 표는 어느 허용 행에도 해당하지 않았음(원문 표 옮김도, 대상 3개↑×축 2개↑ 비교도 아닌 1대상 × 속성 나열) ⑵ 나쁘다고 판정된 §3 표는 "대상 3개↑×축 2개↑"를 형식상 충족해 통과했음 — 허용 조건이 표의 **모양**만 보고 셀의 **해상도**를 보지 않았던 것. 조치 = 허용 표에 `팩트시트(항목·값)` 행 추가(한 대상의 식별 속성 5개↑ 흩어질 때, 절당 1), `셀 해상도` 불릿 신설(표 셀은 독자가 되말할 값까지 — 소스 경로·패키지·모듈·클래스·메서드명은 요약 해상도 밖이라 그 열이 있는 표는 만들지 않고 **도식 + 구성 요소별 불릿 한 줄**로 대체, 구현 좌표는 `참고` 절), 판정 기준에 "셀을 지웠을 때 되말하기에 지장이 있는가" 추가. 연동 = §3 골격 주석, §5 눈 점검 항목, `SKILL.md` 3단계 2번. **기계 점검 미도입** — 백틱 경로 검출은 저장소 슬러그(`pipecat-ai/pipecat`)와 소스 경로를 구분하지 못해 오탐이 확실하므로 눈 점검에 둠. 원 문서 §3 표는 이 개정 시점에 손대지 않음(팀장님 지시 범위 = 스킬)
- **2026-09-05 18:33** — **[원문 요약(digest)] 모드에 도식·표 조건부 허용** — `digest.md` 규칙 2 의 "표는 원문에 표가 있을 때만, 도식은 만들지 않음"을 조건부로 완화하고 `§2 도식·표` 항 신설(허용 조건·형식·상한 표 + 위치·캡션·판정 기준). 근거 = 팀장님 요청('26.9.5) "요약 문서에 이해를 돕는 도식화·테이블 지침을 넣자. 원문에 그림이 있거나 핵심이 되는 테이블 정보가 있으면, 핵심 요약에 필요하다는 판단일 때 생성". 설계 원칙 = **요약자 해석이 되지 않는 선** — 도식은 원문에 그림이 있을 때만 재구성(새 그림 금지, 원문 요소·화살표·라벨만), 표는 원문 표 옮김 또는 대상 3개↑×축 2개↑ 줄글 비교의 재구성(원문에 없는 축·판정 열 금지 — 하우스 §8 표 끝열 판단은 요약자 판단이라 이 모드에서 배제). 상한 = 도식 절당 1·문서 2, 표 절당 1(트랜스포머 3편 원문의 그림이 40여 장이라 "그림이 있으면"만으로는 상한이 필요했음). 캡션 불릿에 원문 그림 번호 표기 의무 — 독자가 원문 대응물을 찾게 함. 연동 = §3 골격에 선택 슬롯, §5 점검표에 도식 수·눈 점검 항목, `SKILL.md` 3단계 2번·4단계 주석, `check.sh --digest` 에 `[도식 수]` 축(코드블록 ≤2). **동반 결함 수정** — `check.sh` 줄글 판정이 코드블록 내부 줄을 거르지 않아 ASCII 도식을 넣으면 그 줄 전부가 줄글 위반으로 잡혔음(fence 상태 추적 없이 ``` 줄만 예외 처리) → awk 에 fence 토글 추가. 첫 적용 = `scratch/DIGEST-TRANSFORMER-SELF-ATTENTION-2026-09-05-1821.md`(도식 2·표 1). 효과는 미검증 — 도식 있음/없음 판본의 이해 대조가 필요
- **2026-09-05 18:40** — **[원문 요약(digest)] 모드 신설** — `references/digest.md`(규칙 7·골격·예시 9편) + `SKILL.md` 1~4단계·적용 범위 표·description 트리거 연동 + `scripts/check.sh --digest` 축 5종(참고 절·본문 링크·시사점 위치·줄글 문단·절 수). 근거 = 팀장님 요청('26.9.5) "긱뉴스 GN⁺(news.hada.io/plus)처럼 긴 원문을 핵심 파악용으로 요약하는 스타일을 스킬에 추가. 웹 링크도 로컬 문서도 입력. 지침만으로는 원하는 스타일이 안 나오고 예시의 효과가 컸으니 사이트 글을 풍부하게 읽어 표본을 발췌". 표본 13편 판독(제품 발표·실험 보고·가이드·장애 경위·보안 공지·통계·디버깅 기록·의견·연구·영상 대담·도구 소개·에세이·비즈니스 비판)에서 귀납 — 골격은 원문 구획을 따르고, 절 안은 불릿만, 어미 `~함/~임/~됨/~음`(하우스 §2.1과 동일 — **doc-writer 가 아니라 이 스킬에 두는 근거**), 수치 보존, 주장은 저자 귀속, 요약자 판단 0. 팀장님 결정 5건 = ⑴ 레이어가 아닌 **네 번째 모드**(골격이 다름) ⑵ **자립성 유지** — 링크는 마지막 `참고` 절과 `related_docs` 에만(GN⁺의 제목 아래 출처 줄·본문 인라인 링크는 채택 안 함) ⑶ **제목은 하우스 명사구**(GN⁺의 헤드라인·의문문 제목 채택 안 함) ⑷ 명칭은 "리뷰"가 아니라 **요약** — 작성자 판단은 마지막 `시사점` 절 하나에 격리 ⑸ 예시는 GN⁺ 요약 본문을 그대로 발췌하되 제목·절 번호·0절·참고 절만 하우스 형식으로 손댐(손댄 범위를 예시마다 명시). GN⁺ 내부 프롬프트(약 10k, 비공개)는 보지 못했으므로 규칙은 표본 귀납(🔴). 효과는 미검증 — 같은 원문을 예시 있음/없음으로 대조해야 판정 가능
- **2026-09-05 17:25** — `SKILL.md` 3단계에 **[지정 문서 파생] 모드 신설**(1단계 모드 줄·적용 범위 표 연동). 근거 = 팀장님 지적('26.9.5) "doc-writer 로 만든 문서의 프론트매터에 관련 산출물과 웹 링크가 있으니, report-style 로 다시 쓸 때 그 자료를 더 들여다보고 작성할 수 있어야 한다". 확인 결과 **경로가 편도였음** — 넘기는 쪽은 갖춰져 있고(doc-writer §6 이 `related_docs` 를 두는 근거로 "본문을 잘라 외부 전달본으로 옮겨도 프론트매터가 출처와 문서 간 관계를 보존한다"를 명시), 받는 쪽인 이 스킬에는 원본 프론트매터를 여는 절차가 없어 [기존 원고 교정]의 surgical 문체 교정으로만 흘렀음(`related_docs`·`출처`·`재료`·`전환` grep 결과 references 4종 전량 미검출). **§1 자립성과 맞물려 실제 손실이 발생하는 자리** — 보고본은 참조 링크를 달 수 없고 필요한 내용을 본문에 발췌·재서술해야 하므로(core.md §1), 무엇을 열지 알려주는 원본 `related_docs` 를 건너뛰면 근거 없이 결론만 옮겨진다. 절차 4스텝 = 프론트매터 선독 → `related_docs` 추적·발췌 → `measured`(수치 기준 시점)·`open_issues`("미확인 — 확인 대기") 이관 → 레이어 골격으로 재작성. **문체 교정이 아니라 새 보고본 작성**이며 원본 파일은 고치지 않음(CLAUDE.md 의 "전환은 재사용 권고" 와 동일 원리). 예외 2건 = 프론트매터·`related_docs` 부재 시 사용자에게 1회 확인, 다중 지정 시 결론을 담은 문서와 근거를 담은 문서의 판별 선행. **규칙 신설 아님** — core.md §1 의 외부 문서 발췌 규칙을 실행 절차로 옮긴 것
- **2026-09-05 15:00** — `scripts/check.sh` 에 **[자립성 절 지시어] 축 신설**. 근거 = 팀장님 지적('26.9.5) "이전 문서는 해당 문구를 넣어 자연스럽게 읽히게 했는데 이건 절 번호로만 표시된다". 기존 `XREF` 는 괄호에 든 정형구(`상세는 §`·`→ §N`·`(§N 참조)`)만 잡아, **서술어에 녹아든 형태**(`§4.2가 지적한`·`§1의 세 축`·`§2에서 판정한`)가 전부 통과했음 — 같은 날 BYOK 문서에 7건을 넣고도 "전 축 0건"으로 보고된 것이 이 구멍. 새 축 `XREF2` 는 절 번호에 조사가 붙은 형태만 잡음. **패턴 선정은 실측으로** — 저장소 md 전수에서 `§[0-9]` 는 2,027건, 헤딩 대조 방식은 2,425건이라 둘 다 과잉이었고(목차 줄·개정 이력 항목까지 적중), 조사 한정 패턴은 348건이며 표본 14건 중 오탐 2~3건(전부 `대상 문서 §4.3의 표` 같은 **외부 문서 절 인용**). 오탐 경고를 출력 줄에 붙여 사람이 판정하게 둠. **한계 = 조사 없는 형태**(`§4.1 노출 경로 점검은 잔존`)는 미검출 — 7건 중 6건 검출. 기존 `XREF` 축은 그대로 두고 나눠 출력(정형구와 지시어는 교정 방법이 달라 섞으면 판독이 어려움). **규칙 신설 아님** — §1.4·§1 본문의 "섹션 간 cross-ref 방향 불문 금지"가 이미 이 형태를 포함하므로, §6 체크리스트의 **예시 형태만 보강**(예시에 정형구만 있던 것이 오독을 부른 자리)
- **2026-09-05 14:50** — 14:30 항이 넓힌 **독자 가정을 "IT·AI 기초 지식까지"로 환원**. 근거 = 팀장님 지적('26.9.5) "새로 만든 문서가 좀 더 난해해졌다. 비전문의 정의를 원래대로 IT나 AI에 기초지식이 있는 정도로". 14:30 개정이 `해당 도메인의 기초 지식`을 가정 범위에 추가했으나 **이는 어느 판본에도 없던 범위**였음 — 8/14 원판은 "지능은 가정하되 도메인 지식은 가정하지 않음", 13:24 개정이 여기에 `IT·AI 기초 상식`만 더한 것이고, 14:30 이 도메인 기초까지 얹으면서 한 단계 넘어감. 실제 피해 = 그 정의로 다시 쓴 BYOK 문서에서 테넌트·RSA-OAEP·일방향 저장 등의 풀이가 빠져 난해해짐. 조치 = 한 줄 원칙과 정의 blockquote 의 가정 범위를 IT·AI 기초까지로 되돌리고 도메인 지식은 가정하지 않음을 명시, `SKILL.md` 1단계 판별 줄도 동일 문구로 교체. **`이 업무를 모름`이라는 업무 기준 표현은 유지**(14:30 의 그 부분은 팀장님 지시였고 환원 대상 아님). 교훈 = 사용자 지적을 규칙에 반영할 때 **직전 판본만 보고 얹지 말고 그 규칙의 개정 계보를 먼저 확인**할 것
- **2026-09-05 14:30** — **[C] 레이어의 독자 정의를 레이블·본문 전체에 일치**시킴. 근거 = 팀장님 지적('26.9.5) "비전문이라는 게 이 업무를 잘 모른다는 것이지, 도메인과 관련 기술의 기초 지식은 있다는 전제여야 함". 13:05 개정으로 한 줄 원칙에는 이미 반영돼 있었으나 **레이블과 나머지 문장은 그대로여서 서로 어긋난 상태**였음 — §7.4 4번이 "**비도메인** 독자로 합격 테스트"라고 적혀 원칙과 정면으로 배치. 조치 넷 ⑴ `readability.md` 헤더에 정의 blockquote 신설("비전문" = 이 업무·과제를 모름, IT·AI와 해당 도메인 기초는 전제. 독자를 낮춰 잡으면 분량만 늘고 정밀도가 떨어짐) ⑵ 한 줄 원칙의 가정 범위에 **해당 도메인의 기초 지식** 추가(기존은 IT·AI 기초 상식까지만) ⑶ `비도메인 독자`·`이 분야를 모르는`(2곳)·`그 분야 밖 독자`를 업무 기준 표현으로 교체 ⑷ 레이어를 고르는 자리인 `SKILL.md` 1단계 판별 블록에 정의 한 줄 — readability.md 를 읽기 **전에** [C] 여부를 판정하므로 그 자리에 필요. **레이블 `비전문 독자` 자체는 유지**(팀장님 판정 — 13곳 일괄 개명 대신 정의 명시)
- **2026-09-05 14:22** — **업데이트 로그를 `core.md` 에서 이 파일로 분리**하고 `core.md` 에는 포인터 한 줄만 남김. 근거 = 팀장님 지적('26.9.5) "이 스킬로 뭔가 할 때는 불필요한 정보". 실측으로 로그 21개 항목이 `core.md` 37,163자 중 **7,630자(20.5%)** 를 차지했고, `core.md` 는 `SKILL.md` 2단계 표가 "공통(항상)"으로 지정해 매 작업마다 전량 로드되는데 1~5단계 어디에서도 로그를 읽지 않았음. 형제 스킬 `doc-writer` 의 `writing-principles.md` 에는 로그 절이 아예 없어 하우스 요건이 아님도 확인. **삭제가 아니라 이동** — 21개 항목이 규칙마다 지적자·근거·적용 지점·오탐 확인 결과를 담고 있어 규칙을 고칠 때 같은 논의를 되풀이하지 않게 막는 자료(최고(最古) 항목 2026-05-24). 연동 = `core.md` 독립 문서 선언 줄의 이력 위치 표기, `SKILL.md` 2단계 표에 "규칙 개정 시에만" 행 신설. `core.md` 는 29,655자로 줄어듦(-20.2%). **§3.1·§3 구조 규칙의 "장기 문서는 `## 0. 업데이트 로그`를 둔다"는 문서 작성 규칙이므로 존치** — 이 이동과 무관
- **2026-09-05 14:22** — [C] 레이어 예시 읽기 분량을 **2~3편으로 통일**. 근거 = 팀장님 지시('26.9.5) "한두 편보다는 좀 더 풍부하게". 동시에 기존 불일치 해소 — `SKILL.md` 2단계 표만 "3~4편", 나머지 네 곳(`SKILL.md` 3단계, `readability.md` 헤더·§7.4, `core.md` §0 요약표 7행)은 "한두 편"으로 갈려 있었음. 다섯 곳 전부 "2~3편"으로 맞춤
- **2026-09-05 13:05** — readability.md §7 **독자 가정과 원어 병기 기준 2줄 보강**. ⑴ 한 줄 원칙의 "도메인 지식은 가정하지 않음"을 "IT·AI 기초 상식은 가정하되 문서가 다루는 제품·API·기술의 지식은 가정하지 않음"으로 좁힘 ⑵ 규칙 2에 원어 괄호 병기 기준 추가 — 독자가 실제 작업 중 다시 마주칠 이름(설정 키·오류 문구·공식 문서 제목)만 남기고, 설명용 원어는 병기하지 않음. 근거 = Fable 5.1 프롬프팅 가이드 [C] 작성('26.9.5)에서 `adaptive thinking`을 "생각하는 기능"으로만 풀어 팀장님이 원어를 되물음. 규칙 목록을 늘리지 않고 기존 두 문장만 손댐(doc-writer 지침 과다로 성능이 떨어진 선례 회피)
- **2026-09-05 12:50** — `references/examples.md` **3편 → 8편 확장**. 추가 장르 다섯 = 판단·권고(MCP 도입 검토)·경위·원인(2022 카카오 장애)·숫자 해석(MMLU·HumanEval)·통념 교정(RAG와 환각)·절차 안내(Claude Code 설치). 근거 = 같은 날 GPT-6 Astra 보고서 작성 시 §위험·§권고 절에 맞출 형판이 없어 예시 1·2를 즉석에서 섞어야 했던 공백. 원문은 팀장님이 AI에 질문해 받은 대화형 답변 5편이며 명사형 종결·인용 번호 제거·후속 제안 삭제·감정 묘사와 IT 동작 구어 교체로 정제, 이해를 단축하는 비유(오픈북 시험·지휘 통제실 등)는 남김. **읽기 절차 변경** — 파일이 168 → 398줄이라 "세 편 전부"를 "장르 표에서 가장 가까운 한두 편"으로 교체(SKILL.md 1·2·3단계, readability.md 헤더·§7.4, §0 요약표 7행 연동). 예시 5·6의 수치(127시간·전문가 평균 80~85% 등)는 원문 그대로이며 미검증 🔴
- **2026-09-05 12:23** — [C] 가독성 레이어에 **예시 기반 작성 도입** — `references/examples.md` 신설(설명형 예시 3편: 제품 소개·두 대상 비교·원리 설명, 명사형 종결로 정제). 근거 = 팀장님 판단('26.9.5) "규칙 목록으로 문체를 추출해 지침으로 만들었더니 재현이 안 됐다. 예시 문서를 주고 같은 문체로 쓰게 하는 쪽이 효과적이라는 경험담을 시도". 예시 원문은 대화형 AI 답변(존댓말)이었으며 명사형으로 다시 쓰고 복사 잔재·인용 번호·후속 제안 마감·수사 수식을 제거. **골격·어조만 베끼고 주제·수치는 옮기지 않음**을 예시마다 헤더로 명시(수치는 미검증 🔴). SKILL.md 1·2·3단계와 readability.md §7.4에 "본문 작성 전 읽음" 절차 연동, §0 요약표 7행 연동. 효과는 미검증 — 같은 주제를 예시 있음/없음으로 새 세션에서 대조해야 판정 가능
- **2026-08-26 08:44** — §3.1 프론트매터 표준에 **`keywords` 필수 필드 신설**(검색용 호칭 3~6개 인라인 배열) 및 **`description` 한 줄 완결 명시**. 근거 = 저장소 실측('26.8.26) — 동일 대상을 부르는 말이 갈릴 때 `description` 산문만으로는 한 호칭에서만 검색에 걸림(MyK PoC 문서 대상 `MyK` 0건 / `My K` 1건 / `Agent Composer` 3건, 예지정비 문서 대상 `예지정비` 4건 / `FDD` 0건). `description` 은 결론 한 문장 원칙이라 별칭 나열을 겸할 수 없어 전용 필드로 분리. **동반 = doc-writer 스킬 동시 개정**(같은 필드·같은 정의 — 두 스킬 배타 적용이므로 표준이 갈리면 문서 유형에 따라 검색 표면이 사라짐). `scripts/check.sh` **[프론트매터 구조] 축 신설**(필수 필드·`keywords` 형식·`description` 한 줄) — 문체 축의 프론트매터 제외는 유지. §0 요약표 3.1행·§6 체크리스트·골격 도해 연동. **`type`·`status` 열거 검사는 미도입** — 본 스킬은 두 필드를 열린 값으로 두므로(`status` 는 `active`·`draft`·`archived` **등**) 닫힌 목록 강제는 규칙에 없는 제약
- **2026-08-08 10:50** — §2.8 IT 동작 동사 표에 **`가르다 / 가르고 / 가른다 / 가른 / 갈라` 추가**(→ 나눈다 / 분리한다 / 구분한다, 맥락별 선택). 근거 = 팀장님 지적('26.8.8) "가르고·가른다 이 표현 대신 나누다·분리하다 등으로 문맥에 따라 사용". 칼로 쪼갠다는 **물리 동작 비유**라 무엇을 했는지가 드러나지 않고, 이미 회피어인 `갈리다`(2026-08-04 등재)의 능동형이어서 수동만 막고 능동은 열려 있던 상태였음. 적용 = Anthropic 공식 SRE·RCA Agent 사례 분석 2곳 교체(프로세스를 가른 → 나눈 / 단계로 가르고 → 분리하고), 스킬 자체 문안 3곳 교체(core §0 요약표 2.5행·§2.5 판정 기준 / executive §Executive Summary). `scripts/check.sh` **[물성 비유 동사] 축을 [물성·분할 비유 동사]로 확장**해 `가르다`·`갈리다` 양 계열과 파생형 `갈림`·`{두·세·네·여러} 갈래` 등재 — 축 신설 대신 누적한 이유는 2026-08-07 항이 밝힌 설계 의도(같은 성격의 동사를 이 축에 누적)를 따른 것. 오탐 0건 확인(저장소 md 전수 대조 — `가르치다`는 자형이 달라 미검출, 명사 `갈래` 단독은 정상 용법이 있어 수량 관형사가 앞선 형태만 등재). **동반 정리 = 미완 항목 마감** — 2026-08-06 항이 "`두 가지 선택지` → `두 가지 방법` 교체"를 적었으나 §2.8 요약표(2.8행)와 §6 체크리스트에는 반영되지 않아 회피어를 권장어로 제시하던 상태가 남아 있었음. 두 곳 모두 `두 가지 방법`으로 교체
- **2026-08-07 23:45** — §2.8 IT 동작 동사 표에 **`굳히다 / 굳힘 / 굳혀` 추가**(→ 정의한다 / 구성한다 / 정리한다 / 확정한다). 근거 = 팀장님 지적('26.8.7) "굳힘·굳히다 이 표현 쓰지 마, 정의·생성·적용 등 직관적 표현으로". 액체가 고체가 된다는 **물성 비유**라 문서에서 무엇을 했는지가 드러나지 않고, 같은 자리에 `정의`·`구성`을 넣으면 행위가 특정됨. 적용 = 담당 워크플로 협의 자료 3곳 교체(스킬 번들 정의·스킬로 정의해 실행·스킬로 정의해 전달). `scripts/check.sh`에 **[물성 비유 동사] 축 신설**(`굳히|굳혀|굳힘|굳힐|굳혔` — `굳이`는 형태가 달라 오탐 없음). 축을 별도로 둔 이유 = 기존 [혈통·생물 비유] 축의 의미를 흐리지 않으면서, 같은 성격의 동사가 추가로 지적되면 이 축에 누적하기 위함. §6 체크리스트 IT 동작 구어 줄 연동
- **2026-08-06 08:30** — §2.5 격식 한자어에 **`선택지` 추가**(→ 방법 / 방식 / 대안). 근거 = 팀장님 지적('26.8.6) "선택지라는 게 잘 안 쓰는 말인데, 구체적으로 약간만 풀어쓴다면". 회의에서 입으로 나오지 않는 문어체이며, 뒤에 예시가 이어지는 자리에서는 단어 자체가 정보를 더하지 않음. **동반 정리 = 스킬 내부 모순 해소** — §2.8 구어 표의 `두 길 / 두 갈래` 행이 권장 대체어로 `두 가지 선택지`를 제시하고 있어, 새 회피어를 권장하던 상태를 `두 가지 방법`으로 교체. 선례 = AO Agent 본부보고 v1.15에서 "중장기 선택지로 확보" → "대안으로 확보"(같은 판단이 이미 한 번 나옴). 적용 = 본부보고 정본 §2-3·§4 비고 2곳 교체. `scripts/check.sh` HANJA에 `선택지` 반영, §0 요약표 2.5행 연동
- **2026-08-05 16:40** — §2.8에 **`불필요한 부정 프레이밍 회피`** 항 신설(표 1행 + 별도 항). 부정으로 지우려는 프레임이 오히려 살아나므로, 제기되지 않은 의혹을 문서가 먼저 꺼내지 않고 긍정 서술만 남긴다. 근거 = 같은 판단이 하루에 두 번 나옴('26.8.5) — ⑴ 팀장님 지시로 "경쟁이 아닌 분업 구도" → "협력 구도" ⑵ AO Agent 정의에 "적용 범위는 특정 시스템에 한정되지 않음" 방어 문구를 넣으려다 철회(범위 오해는 뒤 절의 사실 배치가 해소). **기계 점검은 넣지 않음** — `기능이 아니라 확산 원가에서 차이 발생`처럼 독자의 기본 가정을 교정하는 정당한 대비와 정규식으로 구분되지 않아 오탐이 큼. §6 체크리스트 연동
- **2026-08-05 16:00** — 어휘 3건 보강. ⑴ §2.5에 **`포괄` 추가**(→ 담당/적용/전부 처리) ⑵ §2.8에 **혈통·생물 비유 명사 행 신설**(계보·족보·뿌리·DNA·유전자·태생 → 유형·패턴·계열·구조) ⑶ §2.5에 **"교체 방향은 언제나 쉬운 쪽(역행 금지)"** 상위 원칙 신설 — §2.5·§2.7·§2.8 전 교체에 적용. 근거 = 팀장님 지적 2건('26.8.5) "계보라는 표현도 IT 보고서에는 어울리지 않음, 동일 패턴·동일 유형이 적절" / "'포괄하지 못함'도 어렵다, dry하고 평이한 표현으로". 후자는 **구어(`덮지 못함`)를 지우려다 더 어려운 한자어를 넣은 역행 사례**라 원칙으로 승격. `scripts/check.sh` HANJA에 `포괄`, **[혈통·생물 비유] 축 신설**. §6 체크리스트 연동
- **2026-08-05 15:35** — `§2.9 주체·조직 표기 (제3자 판독성)` 신설 4항. ⑴ 1인칭·소속 대명사(우리·저희·당사) 금지 → 조직명 특정 ⑵ 타 팀 명칭 전체 명칭 유지(약어 규칙 예외) ⑶ 타 조직의 진척·역량 규정 금지(역할·협의 사실까지) ⑷ 미완료 작업 완료형 서술 금지·검증 수치 전제 명시. 근거 = 팀장님 지시 3건('26.8.5, AO Agent 본부보고 게시 과정) — "'우리'는 공식보고서에 부적합, 제3자가 주체를 알아야 함" / "팀 이름은 full name을 적는 게 매너" / "상대팀의 실제 단계를 우리가 함부로 얘기하면 안 됨". 실제 교정 = Summary 라벨 `(우리 Agent)`→`(AO Agent)`, 조직명 축약 5곳, §5-3 "양측 모두 기획 단계" 삭제, 소제목 "무엇으로 대체했는가"→비교 서술. `scripts/check.sh`에 **[1인칭·소속 대명사] 축 추가**. §0 요약표 2.9행·§6 체크리스트·executive §8.5·§8.7 연동
- **2026-08-04 21:55** — §2.8 IT 동작 동사 표에 **`갈리다` 추가**(→ 차이가 발생하다 / 차이 발생 지점). 비교·대조 서술에서 "성격이 갈린다"·"갈리는 지점" 형태로 반복되던 구어 비유를 사실 진술로 교체. 근거 = 팀장님 지시('26.8.4, "잘 안 쓰는 표현"), AO Agent 본부보고 [참고] 글로벌 2사 비교 블록 1곳 교체("갈리는 지점은 제품 형태" → "차이는 제품 형태에서 발생"). §6 체크리스트 연동
- **2026-08-04 20:55** — 타 부서 임원 보고 원문 대조(Biz플랫폼운영센터 AIOps 자료 2건 `880852460`·`880852229`, '26.7.24 박영진 작성) 결과 반영. §3.2에 **행위 서술형 제목 예외**(임원·부문 보고 한정, 규칙 본문은 executive R11), §4.4 **분류 칩** 신설(구 4.4 Blockquote 콜아웃 → 4.5). executive 측은 R11·R2 볼드 2형·R5 대괄호 3용법·R6 지표명 단서·R9 표기 확장·§8.2 표 구현형·§8.4 라벨 구분자 우선순위(콜론 기본)·§8.7 어휘 4군 추가, **엠대시 상한 3 → 1/100줄**(원문 실측 본문 0개). 근거 = 팀장님 결정('26.8.4, 임원 보고 한정 허용·엠대시 최소화). §0 요약표 3.2·4·8행, §6 체크리스트 연동. **같은 날 보강** — R11 첫 적용(AO Agent KT DS ITO 설명자료)에서 "타 시스템 확장을 뒷받침하는" 류 번역체 수식이 발생해, R11에 적용 대상 제한(주장 있는 절만)·회피 수식어(뒷받침하는·기여하는·촉진하는·견인하는)·한 호흡 원칙 3항 추가
- **2026-08-02 19:40** — §2.5 격식 한자어에 **`환류` 추가**(→ 반영 / 되먹임). `scripts/check.sh` HANJA 정규식 반영. 근거 = 팀장님 지시("환류는 어려운 한자어"), AO Agent 본부보고 §6 자가 발전 항목 1곳 교체. §0 요약표 2.5행 연동
- **2026-07-29 08:55** — §2.5 격식 한자어에 **회피 6종 추가**(기제·소관·준용·전건·형상·상회) + **"교체하지 않는 한자어" 허용 목록 신설**(부재·판명·선결·내재화·편입·파급·규명·수렴 등 23종). 판단 기준에 임원 보고용 한 줄 추가("4~50대 IT 경영진이 회의에서 입으로 말하는 단어인가"), 판정이 갈리면 **교체하지 않는 쪽이 기본**. 근거 = AO Agent 본부보고 정비('26.7.29) 22곳 교체 후 팀장님 검수 13곳 원복. `scripts/check.sh` HANJA에 기제·소관·준용·전건 반영(형상은 형상관리 오탐, 상회는 관용구라 정규식 제외). **`주지` 오탐 수정** — `주지` → `주지(시|의)`로 좁혀 "알려주지·넘겨주지" 류 정상 문장이 잡히던 문제 제거(검증: 정상 2문장 통과·"주지시킬"·"주지의 사실" 검출). §0 요약표 2.5행 연동
- **2026-07-11 17:19** — §3.1에 YAML 프론트매터 표준(type·audience·related_docs·created·updated·status·description, description=결론 먼저) 신설, 블록quote 메타블록은 독립 선언·부속 자료로 역할 분리(중복 회피). §0 요약표·§6 체크리스트 연동
- **2026-07-11 17:14** — §1.4 배포용 산출물 자립성(Self-Contained Packaging·Leaky Package 회피)·§3.4 파일 명명 규칙(PREFIX-설명-타임스탬프) 신설(구 글로벌 CLAUDE.md 2항 흡수). §0 요약표·§6 체크리스트 연동
- **2026-07-07 19:45** — §2.8에 "수사·마케팅 카피"·"저수준 명령어·도구 축약" 2행 추가(슬로건·npx류 배제 → 담백한 기술 팩트). §0 요약표·§6 체크리스트·executive §8.7 연동
- **2026-06-28 16:10** — `## 8. 임원·부문 보고 추가 규약` 신설(구 GUIDE-IT-DIVISION-REPORT-WRITING-STYLE §5~부록 통합 — R1~R10·3단 골격·엠대시 빈도 가드·임원 보고 어휘 사전, WORKING 기존 중복 항목 제외). §0 요약표 §8 행 연동
- **2026-05-26 12:35** — §2.8에 "IT 동작 구어체 → 표준 IT 용어" 행 + IT 동작 동사 매핑 표 추가 (깔다·박다·잡다·건드리다·풀리다·뒤집다·꼬리 등). §0 요약표·§6 체크리스트 연동
- **2026-05-25 18:32** — §2.3에 압축 상한선(동료 1회 이해)·과압축 자가진단 2줄 추가. §0 요약표 22행 연동
- **2026-05-24 22:37** — `§2.8 감정·비유·구어 표현 회피 (중립 기술 톤)` 신설. §0 요약표·§6 체크리스트 연동
